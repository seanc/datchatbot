const Endpoints = require('./Endpoints');
const superagent = require('superagent');
const chatBotError = require('./Error');
const Promise = require('bluebird');

class chatBot {
    constructor (token) {
        this.token = token;
    }
}

query (message) {
    
    //it looks so gay so this is empty for now
    
}

module.exports = chatBotClient;